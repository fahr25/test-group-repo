@page "/"

<PageTitle>Home</PageTitle>

<h1>Hello, world!</h1>

<p>Welcome to <em>my</em> new app.</p>

<h2>Default Component</h2>
<div class="card">
    <div class="card-body">
        @* Counter component with a custom increment amount by passing a parameter into it.
        IncrementAmount represents the amount by which the count will be incremented. *@
        <Counter/>
    </div>
</div>
<br />

<h2>Custom Increment Component</h2>
<div class="card">
    <div class="card-body">
        @* Counter component with a custom increment amount by passing a parameter into it.
        IncrementAmount represents the amount by which the count will be incremented. *@
        <Counter IncrementAmount="3"/>
    </div>
</div>